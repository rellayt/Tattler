import React from 'react';
import { Heading, Wrapper } from './NewRoom.styles';

const NewRoom = () => {
  return (
    <Wrapper>
      <Heading>Add user / users to create room</Heading>
    </Wrapper>
  );
};

NewRoom.propTypes = {};

export default NewRoom;
